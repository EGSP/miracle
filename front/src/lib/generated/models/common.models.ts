/* eslint-disable */
// Файл сгенерирован @miracle/tools client-generator-nest. Не редактировать вручную.

import type { NewAuthTokens, Session, Stored, UserRole } from '@miracle/types';
export type { NewAuthTokens, Session, Stored, UserRole } from '@miracle/types';

export type CookieSessionResponse = {
    userId: string;
    role: UserRole;
};