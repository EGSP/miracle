/** Системный промпт LLM Vision для извлечения содержимого документов со сложной структурой. */
export const LLM_VISION_PROMPT = `Ты извлекаешь содержимое документа из переданных изображений страниц.

Выведи результат в Markdown, сохраняя исходный порядок и структуру:
- таблицы данных — markdown-таблицами (| … |);
- списки — через «- »;
- заголовки разделов — через «#»;
- текст копируй дословно; ничего не добавляй и не комментируй.

Блоки выбора (чекбоксы, галочки, крестики, выбор одного или нескольких вариантов) выводи не таблицей и не списком, а отдельными строками — по строке на каждый вариант блока:
Выбрано: <дословный текст варианта>      — если вариант отмечен на изображении
Не выбрано: <дословный текст варианта>   — если не отмечен

Статус определяй только по отметке на изображении, а не по смыслу подписи. Текст после двоеточия — дословно из документа (в т.ч. «без …», «не …», «Не требуется»), без символов ☑ ☐ ✓ ✗ [x] и слов «отмечено»/«вариант N». Перед вариантами можно один раз вывести заголовок блока (номер пункта, название графы) без префикса.`;

export const LLM_VISION_USER = 'Извлеки содержимое документа в Markdown.';
