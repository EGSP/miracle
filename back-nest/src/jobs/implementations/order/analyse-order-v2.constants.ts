/**
 * Тумблеры алгоритма анализа заказа v2. Значения по умолчанию выключают новое поведение —
 * включается осознанно (см. README, раздел «Тумблеры»).
 */

/**
 * Порог токенов для разбиения группы одного ТУ на подгруппы (greedy bin-packing по требованиям).
 * `undefined` → разбиение не применяется, группа = все позиции одного ТУ.
 */
export const GROUP_TOKEN_BUDGET: number | undefined = undefined;

/**
 * Ограничение параллелизма обхода групп. `undefined` → последовательно (одна группа за раз) из-за
 * ограничений Яндекса. Число включит ограниченный параллелизм групп.
 */
export const GROUP_CONCURRENCY: number | undefined = undefined;

/**
 * Ограничение параллелизма обхода слотов внутри группы. Заведён «на будущее»: слоты сейчас всегда
 * последовательны (см. `analyse-group-designation.job.ts`), поэтому константа пока не используется.
 */
export const SLOT_CONCURRENCY: number | undefined = undefined;
